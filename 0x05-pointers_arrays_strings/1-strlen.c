#include "main.h"
#include <stdio.h>

/**
 * _strlen - shows length of string
 * Return: string length
 * @s: parameter
 */

int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)
		return (n);
}
